# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# FIREBASE_SERVICE_ACCOUNT: JSON string of the Firebase Service Account.
# Required for sending push notifications from the server.
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", ...}'

# VITE_FCM_VAPID_KEY: The public VAPID key from Firebase Console -> Cloud Messaging.
# Required for the client to subscribe to push notifications.
VITE_FCM_VAPID_KEY="MY_VAPID_PUBLIC_KEY"

# SUPABASE CONFIGURATION:
# VITE_SUPABASE_URL: Your Supabase Project URL (e.g. https://xyzcompany.supabase.co)
VITE_SUPABASE_URL=""

# VITE_SUPABASE_ANON_KEY: Your Supabase Public Anon Key
VITE_SUPABASE_ANON_KEY=""

# SUPABASE_SERVICE_ROLE_KEY: Optional backend key for administrative tasks / migrations
SUPABASE_SERVICE_ROLE_KEY=""
